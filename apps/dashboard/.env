# Absolute prod backend URL. Baked into the Next.js build at deploy
# time. For compose/local dev, use a branch or override
# `NEXT_PUBLIC_API_URL=auto` in a `.env.local` (which stays gitignored).
NEXT_PUBLIC_API_URL=https://api.harrokrog.com
NEXT_PUBLIC_AUTH_MODE=google
