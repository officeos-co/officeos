query SupportedModels {
  supportedModels {
    id
    displayName
    provider
    isDefault
  }
}

query Providers {
  providers {
    id
    name
    displayName
    configured
    configuredAt
    models
  }
}
