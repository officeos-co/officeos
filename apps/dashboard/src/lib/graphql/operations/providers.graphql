query SupportedModels {
  supportedModels {
    id
    displayName
    isDefault
  }
}

query Providers {
  providers {
    id
    name
    displayName
    configured
    configuredAt
    models
  }
}
