node_modules
.next
coverage
cypress/screenshots
cypress/videos
npm-debug.log*
.git

# NOTE: .env is intentionally NOT excluded — the dashboard image is
# self-contained and bakes the committed `.env` in at build time.

