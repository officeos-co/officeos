node_modules/
.next/
.env.local
# NOTE: .env is intentionally NOT gitignored — the dashboard image is
# self-contained and bakes the committed .env in at build time. Same
# pattern as apps/backend/.env. Any real secrets MUST go in .env.local
# (which is gitignored) or via runtime config.
out/
dist/

# clerk configuration (can include secrets)
/.clerk/
next-env.d.ts
