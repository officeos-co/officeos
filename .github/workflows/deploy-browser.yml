name: Deploy Browser Service

# TEMPORARY: Main pushes deploy to production even without version tags.
# Change this back to tag-gated release promotion before formal prod releases.
on:
  push:
    branches:
      - main
    paths:
      - "packages/auto-browser/**"
      - "k8s/staging/browser.yaml"
      - "k8s/prod/browser.yaml"
      - ".github/workflows/deploy-browser.yml"
    tags:
      - "v*"

concurrency:
  group: deploy-browser-prod
  cancel-in-progress: false

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: harkro123
          password: ${{ secrets.DOCKER_PAT }}

      - name: Determine version
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF#refs/tags/}"
            echo "tag=${VERSION}" >> "$GITHUB_OUTPUT"
            echo "Building tagged release: ${VERSION}"
          else
            echo "tag=latest" >> "$GITHUB_OUTPUT"
            echo "Building latest from main branch"
          fi

      - name: Build and push controller image
        uses: docker/build-push-action@v6
        with:
          context: packages/auto-browser/controller
          push: true
          tags: harkro123/eaos-auto-browser-controller:${{ steps.version.outputs.tag }}
          cache-from: type=gha,scope=auto-browser-controller
          cache-to: type=gha,scope=auto-browser-controller,mode=max

      - name: Build and push browser-node image
        uses: docker/build-push-action@v6
        with:
          context: packages/auto-browser/browser-node
          push: true
          tags: harkro123/eaos-auto-browser-node:${{ steps.version.outputs.tag }}
          cache-from: type=gha,scope=auto-browser-node
          cache-to: type=gha,scope=auto-browser-node,mode=max

  deploy:
    needs: build
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Configure kubectl
        env:
          KUBE_SERVER: ${{ secrets.KUBE_SERVER }}
          KUBE_TOKEN: ${{ secrets.KUBE_TOKEN }}
        run: |
          kubectl config set-cluster nova --server="${KUBE_SERVER}" --insecure-skip-tls-verify
          kubectl config set-credentials ci --token="${KUBE_TOKEN}"
          kubectl config set-context nova --cluster=nova --user=ci --namespace=default
          kubectl config use-context nova

      - name: Deploy staging
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        run: |
          kubectl apply --validate=false -f k8s/staging/browser.yaml
          kubectl rollout restart deployment/eaos-browser-staging

      - name: Deploy prod
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            TAG="${{ github.ref_name }}"
          else
            TAG="latest"
          fi
          sed -i "s|IMAGE_TAG|${TAG}|g" k8s/prod/browser.yaml
          kubectl apply --validate=false -f k8s/prod/browser.yaml
          kubectl rollout restart deployment/eaos-browser-prod
