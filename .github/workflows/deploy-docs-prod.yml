name: Deploy Docs Prod

on:
  push:
    branches:
      - main
    paths:
      - 'apps/docs/**'
      - 'docs/**'
      - 'k8s/docs.yaml'
      - '.github/workflows/deploy-docs-prod.yml'

concurrency:
  group: deploy-docs-prod
  cancel-in-progress: false

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: harkro123
          password: ${{ secrets.DOCKER_PAT }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/docs/Dockerfile
          push: true
          tags: harkro123/eaos-docs:latest
          cache-from: type=gha,scope=docs
          cache-to: type=gha,scope=docs,mode=max

  deploy:
    needs: build
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Apply manifest and restart docs
        env:
          KUBE_SERVER: ${{ secrets.KUBE_SERVER }}
          KUBE_TOKEN: ${{ secrets.KUBE_TOKEN }}
        run: |
          .github/scripts/configure-kubectl.sh
          kubectl apply --validate=false -f k8s/docs.yaml
          kubectl rollout restart deployment/eaos-docs-prod
          kubectl rollout status deployment/eaos-docs-prod --timeout=180s
