# BOOTSTRAP.md

This file carries the operator's system prompt for this specific agent. The
text below was injected from the backend's `AgentBootstrapPayload.systemPrompt`
on first boot. Edits to this file persist across restarts (the template is
never rewritten once present).

## Operator instructions

{{prompt}}
