import { describe, it } from "bun:test";

describe("search", () => {
  describe("search", () => {
    it.todo("should pass q param to /messages list endpoint");
    it.todo("should return snippet, from, subject, date for each message");
    it.todo("should fetch metadata for each result individually");
  });
});
