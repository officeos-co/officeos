{
  "name": "@harro/skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "for d in */; do [ -f \"$d/skill.test.ts\" ] && (cd \"$d\" && echo \"Testing $d\" && bun test) done",
    "test:skill": "cd $SKILL && bun test"
  }
}
