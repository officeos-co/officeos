# ─────────────────────────────────────────────────────────────────────
# Enterprise Agent OS — website (Next.js landing page)
#
# Public-facing marketing site. Separate from the dashboard.
# ─────────────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eaos-website-prod
  namespace: default
  labels:
    app: eaos-website-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eaos-website-prod
  template:
    metadata:
      labels:
        app: eaos-website-prod
    spec:
      containers:
        - name: website
          image: harkro123/eaos-website:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
              protocol: TCP
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: eaos-website-prod
  namespace: default
  labels:
    app: eaos-website-prod
spec:
  type: ClusterIP
  selector:
    app: eaos-website-prod
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
