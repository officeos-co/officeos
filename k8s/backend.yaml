# ─────────────────────────────────────────────────────────────────────
# Enterprise Agent OS — backend stack
#
# Everything the backend needs to run: RBAC, the backend PVC,
# and the backend Deployment + Service. Postgres is external (REDACTED_DB_HOST_IP).
#
# Frontend lives in ./frontend.yaml. Credentials are hardcoded inline
# to match the self-contained pattern used in docker-compose.yml —
# rotate by editing here and re-applying.
# ─────────────────────────────────────────────────────────────────────

# ==============================================================
# RBAC — backend needs this SA to spawn per-agent zeroclaw pods
# and clean up PVCs on agent deletion. Without the SA the backend
# Deployment's ReplicaSet can't admit any Pod (it references
# `serviceAccountName: dashboard-backend`).
# ==============================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dashboard-backend
  namespace: default
  labels:
    app: eaos

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: zeroclaw-manager
  namespace: default
  labels:
    app: eaos
rules:
  - apiGroups: [""]
    resources:
      - pods
      - pods/log
      - services
      - persistentvolumeclaims
      - configmaps
    verbs: ["get", "list", "create", "update", "patch", "delete", "watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: zeroclaw-manager-binding
  namespace: default
  labels:
    app: eaos
subjects:
  - kind: ServiceAccount
    name: dashboard-backend
    namespace: default
roleRef:
  kind: Role
  name: zeroclaw-manager
  apiGroup: rbac.authorization.k8s.io

---
# ==============================================================
# Postgres — hosted externally on REDACTED_DB_HOST_IP
# Databases: eaos_prod (production), eaos_staging (staging)
# User: contentmachine — connection strings in appsettings.json
# ==============================================================
# ==============================================================
# Backend DataProtection keys PVC
#
# v2 backend (dotnet) persists DataProtection keys under /data.
# Without this volume keys would vanish on every pod restart,
# invalidating every encrypted provider API key. The database
# is external Postgres on REDACTED_DB_HOST_IP.
# ==============================================================
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: eaos-backend-data
  namespace: default
  labels:
    app: eaos
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi

---
# ==============================================================
# Backend (v2 dotnet / legacy FastAPI image)
# ==============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eaos-backend-prod
  namespace: default
  labels:
    app: eaos-backend-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eaos-backend-prod
  template:
    metadata:
      labels:
        app: eaos-backend-prod
    spec:
      serviceAccountName: dashboard-backend
      containers:
        - name: backend
          image: harkro123/eaos-backend:latest
          imagePullPolicy: Always
          env:
            - name: ENVIRONMENT
              value: production
          ports:
            - containerPort: 8000
              name: http
              protocol: TCP
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              memory: "256Mi"
              cpu: "1000m"
            limits:
              memory: "512Mi"
              cpu: "2000m"
          livenessProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: eaos-backend-data

---
apiVersion: v1
kind: Service
metadata:
  name: eaos-backend-prod
  namespace: default
  labels:
    app: eaos-backend-prod
spec:
  type: ClusterIP
  selector:
    app: eaos-backend-prod
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000

---
# ==============================================================
# Skill Runtime (Node.js — executes SDK-defined skills)
#
# Bundles first-party skills (notion, github, google) and exposes
# an HTTP API for manifest discovery and skill execution.
# The backend dispatches skill calls here via SkillRuntimeClient.
# ==============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eaos-skill-runtime
  namespace: default
  labels:
    app: eaos-skill-runtime
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eaos-skill-runtime
  template:
    metadata:
      labels:
        app: eaos-skill-runtime
    spec:
      containers:
        - name: skill-runtime
          image: harkro123/eaos-skill-runtime:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
              name: http
              protocol: TCP
          env:
            - name: PORT
              value: "3001"
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: eaos-skill-runtime
  namespace: default
  labels:
    app: eaos-skill-runtime
spec:
  type: ClusterIP
  selector:
    app: eaos-skill-runtime
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
