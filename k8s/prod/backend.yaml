# ─────────────────────────────────────────────────────────────────────
# Enterprise Agent OS — production backend stack
#
# Deployed on version tag push (v*). Image tags replaced by CI.
# ─────────────────────────────────────────────────────────────────────

# ==============================================================
# MinIO workspace object storage — prod
# ==============================================================
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: eaos-minio-prod-data
  namespace: default
  labels:
    app: eaos-minio-prod
    env: prod
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 100Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eaos-minio-prod
  namespace: default
  labels:
    app: eaos-minio-prod
    env: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eaos-minio-prod
  template:
    metadata:
      labels:
        app: eaos-minio-prod
        env: prod
    spec:
      containers:
        - name: minio
          image: minio/minio:latest
          args: ["server", "/data"]
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: eaos-backend-prod-secrets
                  key: WORKSPACESTORAGE__ACCESSKEY
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: eaos-backend-prod-secrets
                  key: WORKSPACESTORAGE__SECRETKEY
          ports:
            - containerPort: 9000
              name: s3
              protocol: TCP
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "2000Mi"
              cpu: "2000m"
          livenessProbe:
            httpGet:
              path: /minio/health/live
              port: 9000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /minio/health/ready
              port: 9000
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: eaos-minio-prod-data

---
apiVersion: v1
kind: Service
metadata:
  name: eaos-minio-prod
  namespace: default
  labels:
    app: eaos-minio-prod
    env: prod
spec:
  type: ClusterIP
  selector:
    app: eaos-minio-prod
  ports:
    - name: s3
      protocol: TCP
      port: 9000
      targetPort: 9000

---
# ==============================================================
# Backend DataProtection keys PVC — prod
# ==============================================================
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: eaos-backend-prod-data
  namespace: default
  labels:
    app: eaos
    env: prod
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi

---
# ==============================================================
# Backend — production
# ==============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eaos-backend-prod
  namespace: default
  labels:
    app: eaos-backend-prod
    env: prod
spec:
  replicas: 1
  progressDeadlineSeconds: 900
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: eaos-backend-prod
  template:
    metadata:
      labels:
        app: eaos-backend-prod
        env: prod
    spec:
      serviceAccountName: dashboard-backend
      initContainers:
        - name: fix-data-perms
          image: busybox:1.36
          command: ["sh", "-c", "chown -R 1654:1654 /data"]
          volumeMounts:
            - name: data
              mountPath: /data
          securityContext:
            runAsUser: 0
      containers:
        - name: backend
          image: harkro123/eaos-backend:IMAGE_TAG
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: eaos-backend-prod-secrets
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Production
            - name: REDIS
              value: "eaos-redis:6379,defaultDatabase=0"
            - name: FRONTEND_ORIGIN
              value: "https://dashboard.officeos.co"
            - name: DATA_PROTECTION_KEY_PATH
              value: "/data/dp-keys"
            - name: KUBERNETES__NAMESPACE
              value: "default"
            - name: KUBERNETES__IMAGE
              value: "harkro123/eaos-pod-executor:latest"
            - name: WORKSPACESTORAGE__ENDPOINT
              value: "http://eaos-minio-prod:9000"
            - name: WORKSPACESTORAGE__BUCKET
              value: "eaos-workspaces-prod"
            - name: WORKSPACESTORAGE__ACCESSKEY
              valueFrom:
                secretKeyRef:
                  name: eaos-backend-prod-secrets
                  key: WORKSPACESTORAGE__ACCESSKEY
            - name: WORKSPACESTORAGE__SECRETKEY
              valueFrom:
                secretKeyRef:
                  name: eaos-backend-prod-secrets
                  key: WORKSPACESTORAGE__SECRETKEY
            - name: CHANNEL_SERVICE_URL
              value: "http://localhost:3100"
            - name: BROWSER_SERVICE_URL
              value: "http://eaos-browser-prod:8000"
            - name: BROWSER_PUBLIC_VIEW_BASE_URL
              value: "https://browser.officeos.co"

            - name: POSTHOG__HOST
              value: "https://eu.i.posthog.com"
          ports:
            - containerPort: 8000
              name: http
              protocol: TCP
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              memory: "1000Mi"
              cpu: "1000m"
            limits:
              memory: "2000Mi"
              cpu: "2000m"
          startupProbe:
            httpGet:
              path: /api/health
              port: 8000
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 60
          livenessProbe:
            httpGet:
              path: /api/health
              port: 8000
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/health
              port: 8000
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
        # ── Channel gateway sidecar ───────────────────────────────
        - name: channel-gateway
          image: harkro123/eaos-channel-gateway:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3100
              name: ch-gateway
              protocol: TCP
          env:
            - name: PORT
              value: "3100"
            - name: BACKEND_URL
              value: "http://localhost:8000"
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3100
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 3100
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: eaos-backend-prod-data

---
apiVersion: v1
kind: Service
metadata:
  name: eaos-backend-prod
  namespace: default
  labels:
    app: eaos-backend-prod
    env: prod
spec:
  type: ClusterIP
  selector:
    app: eaos-backend-prod
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
